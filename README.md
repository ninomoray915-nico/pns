# NINO Portfolio Admin

1. 打开 `admin/assets/js/config.js`
2. 将占位文字替换为完整的 `sb_publishable_` Key
3. 把整个 `admin` 文件夹上传到 `pns` 仓库根目录
4. 访问：
   `https://ninomoray915-nico.github.io/pns/admin/login.html`

只能填写 Publishable key，绝不能填写 `sb_secret_` Secret key。
